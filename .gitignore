venv
__pycache__
log
# Treino completo gera ~313 MB de checkpoints intermediários — fora do git.
# Os modelos finais (.zip ~3 MB cada) e finalize_results.json ficam versionados.
data/*_checkpoints/
*.stackdump
